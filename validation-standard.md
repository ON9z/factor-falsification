# 验证标准：10 道闸门 + 防自欺机器

这是 FinAgent 用「最严标准」判一个因子生死的完整定义。核心思想：**任一闸门不过 = 整体 FAIL**，
避免「只盯单指标（如 test 超额）」的爬榜式自欺（AAR 几何平均计分的教训）。

---

## 一、数据口径（先于闸门，是一切的前提）

| 项 | 规则 | 防的是什么 |
|---|---|---|
| 入场 | 信号日 t 的**下一交易日开盘价** `open[t+1]` | T+1 现实；用收盘价算因子、收盘价买 = 未来函数 |
| 出场 | 第 t+h 交易日收盘 `close[t+h]` | 持有到 h 期 |
| 前向收益 | `fwd_ret = close[t+h] / open[t+1] − 1` | — |
| 基准 | 当日全市场**等权**均值 fwd_ret | 等权市场组合（不是市值加权指数） |
| 时点宇宙 | 只算当时可交易的 A 股（剔 ETF/基金），退市票按 `delist_date` 剔除 | 幸存者偏差 |
| 涨停剔除 | 入场日一字/封死涨停买不进 → 该样本不计 | 可执行性 |
| 跌停顺延 | 出场日跌停卖不出 → 顺延到首个可卖日收盘 | 缺陷 3a：别假设跌停日能卖 |
| 交易日对齐 | 前向收益按全局交易日历，不用 raw shift | 停牌断档错位 |
| 无泄露 | 因子只用过去（`.rolling`/`.shift` 因果）；横截面 rank 只在自己当日 | ML 泄漏 |

---

## 二、10 道闸门（`_verdict` 的定义）

| # | 闸门 | 判据 | 防的「自欺」 |
|---|---|---|---|
| 1 | test 超额有统计意义 | test 超额 > 0 且 `|t| ≥ 2` | 只看正负不看显著 |
| 2 | **动态成本后净超额显著为正** | `excess_net > 0` 且 `net_t ≥ 2` | 「毛显著、净不显著」的因子 |
| 3 | train/test 同号 | 样本外不反转 | 只在训练段有、样本外翻脸 |
| 4 | 子区间符号一致 | `sign_consistent_frac ≥ 0.75` | 单段运气 |
| 5 | 置换检验 | `p < 0.05`（未达置换门槛 t<1.5 直接记 FAIL） | 横截面噪声 |
| 6 | （仅组合）增量 | vs 最强单分量 `increment > 0` | 组合只是单因子的马甲 |
| 7 | **DSR 去膨胀** | `DSR ≥ 0.95`（T = 累计搜索次数） | 多重检验 / 搜索自欺 |
| 8 | 覆盖率哨兵 | 覆盖 `≥ 20%` | 拿覆盖子集冒充全宇宙 alpha |
| 9 | 缺失机制随机 | 覆盖子集无市值/行业/ST 系统偏倚 | 覆盖子集是个「别的因子」 |
| 10 | 数据契约 | 时点宇宙 / 价格健全 / 因子有限，任一违反 = 数据 bug | 脏数据当信号 |

> 注意第 2 门是「**净** t ≥ 2」，不是旧版的「净 > 0」：后者会放过「毛显著（t≥2）但净不显著」
> 的因子（如 `analyst_upside` 净 +0.114% t=1.11）。显著性必须落在**扣费后**的序列上。

---

## 三、防自欺机器（闸门背后的原理）

### 1. 动态摩擦（Amihud 2002 非流动性 + 中国 size 流动性溢价）

小成交额（小市值）股票的真实往返成本远高于大盘股。模型：

```
cost_i = BASE_COST + ILLIQ_COST × median_amount_day / amount_i  (封顶 COST_CAP)
```

- `BASE_COST = 0.001`（印花税 0.05% 卖 + 佣金 ~0.05% + 过户费）
- `ILLIQ_COST = 0.001`（中位成交额股 +0.10% 冲击）
- `COST_CAP = 0.0075`（极端微盘封顶 0.75%）

> 关键教训：成本模型**高估 2×** 会让「净显著」变成「净不显著」。09-05 修正了
> `BASE/ILLIQ/CAP` 的算术错误（注释写 0.10% 但常量是 0.2%），重跑后 `size` 从 h5/h10 净正
> 掉到只有 h20 净正。

### 2. Deflated Sharpe Ratio（Bailey & López de Prado 2014）

在同一宇宙上搜了 N 条假设后，纯噪声因子也会有 `E[max t] ≈ sqrt(2 ln N)` 的「伪显著」极值。
DSR 把观测 Sharpe 按 N **去膨胀**，只有显著高于「N 次试验的期望极值」才算真技能：

```
DSR = P[ SR_true > SR_threshold(T) ]
```

- `T` = **累计**候选计数，**持久化到磁盘跨 session 累计**——搜索越久，门槛越高。
- `SR_threshold` 由观测 Sharpe、样本长度、峰度/偏度、T 一起决定（Acklam 逆 CDF）。
- 判据 `DSR ≥ 0.95`。

这是「诚实搜索」的核心：你试的越多，越难靠运气过关。

### 3. 覆盖率哨兵 + 缺失机制审计

- **覆盖率**：每个交易日「可交易宇宙中因子值非空」占比的均值 < 20% = 覆盖子集因子
  （如研报目标价只覆盖大盘股），其 PASS 只在子集内成立，**不可外推到全宇宙**。
- **缺失机制**：覆盖子集是否系统性偏倚（市值/行业/ST）——偏倚即降级，因为那说明
  「有数据的股票」本身就是一个不同的（更可能是有偏差的）子群。

### 4. 中性化（可选报告项）

`neutralize='sector,size'` 逐日对因子做行业/市值中性化，报风险调整后 alpha。
是报告项不是判决项——它回答「这个超额是行业/市值暴露还是因子本身的贡献」。

---

## 四、这条标准是怎么来的（从踩坑中长出来）

1. **数据标度铁律**：百分比 vs 比值不能混（`dv_ttm` 是比值不是百分比）。
2. **复权口径**：`qfq` 前复权日期格式与库不一致会静默重拉 278 万行；断点离群会伪造 `change_pct`。
3. **ML 泄漏**：日线/分钟模型重训泄漏把 AUC 0.711 → 真值 0.510。
4. **同义反复指标**：用「生成信号当日涨跌」判信号对错 → 对动量信号恒真 → 假 100%。
5. **浮点精度**：float32 下采样 rank 选股会出现 0 翻转。
6. **幸存者偏差**：只回补存活股票，`size` 的「超额」在加入退市票后转负。

这些坑**每一个**都在 `factor-graveyard.md` 里对应着一个或多个因子的「死因」。标准不是
凭空设计的，是被这些坑反复教训出来的。
